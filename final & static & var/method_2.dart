///<some-data-types>
///Represents a specific data type (e.g., int, String, double, etc.) that you explicitly specify for the variable.
///The variable's type remains fixed and cannot be changed.
///The variable must be initialized with a value of the specified data type.

int age = 25;
String name = "Alice";

///var
///Allows the type of the variable to be inferred automatically based on the assigned value.
///The type of the variable is determined at compile-time based on the assigned value.
///The variable must be initialized with a value at the point of declaration.

var age1 = 25;
var name1 = "Alice";
